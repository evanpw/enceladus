impl Int
    def eq(self: Int, other: Int) -> Bool
        return self == other

impl [Int]
    def eq(self: [Int], other: [Int]) -> Bool
        return self.head() == other.head()

x := 1i
y := 2

if x.eq(y)
    print $ "Same"
else
    print $ "Different"

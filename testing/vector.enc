# Int vector
v := []

for i in 1 til 11
    v.append(i)

drop $ v.pop()
drop $ v.pop()

total := 0
for i in 0 til v.size
    total += v[i]

# Boolean vector
v2 := []
v2.append(True)
v2.append(False)

if v2[0]
    total += 1

println $ show(total)

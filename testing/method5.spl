impl [T]
    def myAt(self: [T], n: Int) -> T
        xs := self
        while n > 0
            xs = xs.tail()
            n -= 1

        return xs.head()

impl [Int]
    def myAt(self: [Int], n: Int) -> Int
        return 0

impl List<T>
    def myAt(self, n: Int) -> T
        xs := self
        while n > 0
            xs = xs.tail()
            n -= 1

        return xs.head()

impl List<Int>
    def myAt(self, n: Int) -> Int
        return 0

data MyPair<T> = MyPair(T, T)

x := MyPair(1, 2)
y := MyPair(True, False)

let MyPair(a, b) := x
let MyPair(c, d) := y

result := b
if c
    result += 1

def identity(x: T) -> T
    return x

result += identity(3)

if not $ identity(False)
    result += 1

print $ show(result)

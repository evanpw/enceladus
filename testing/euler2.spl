# Solves Project Euler 2

total := 0

last := 1
current := 2
while current < 4000000
	if current % 2 == 0
		total += current

    # TODO: Add this syntax
    # last, current = current, current + last

    next := last + current
	last = current
	current = next

println $ show(total)

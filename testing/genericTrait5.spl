def genericSum(xs: T) -> UInt8 where T: Iterator<UInt8>
    total := 0

    for x in xs
        total += x

    return total

println $ show $ genericSum("hello".iter()) as Int
println $ show $ genericSum([1u8, 2, 3, 4]) as Int

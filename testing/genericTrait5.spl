def genericSum(xs: T) -> Int where T: Iterable<Char>
    total := 0

    for x in xs
        total += x as Int

    return total

println $ show $ genericSum $ "hello".iter()
println $ show $ genericSum $ ['a', 'b', 'c', 'd'].iter()

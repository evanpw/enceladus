def triggerGC() -> Unit
    xs := range(1, 1000000).toList()

def only<T>(value: T) -> [T]
    x := value
    ys := Nil
    triggerGC()
    ys = ys.insert(x)

    return ys

print $ show $ only(4).head()

# Solves Project Euler #29
import BigInt
import List
import String

values := []
for a = 2 to 100
    for b = 2 to 100
        n := bigPower a b

        values = n :: values

values = uniquify cmpList values
print $ show $ length values

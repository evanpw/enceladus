# Solves Project Euler #29
import BigInt
import List
import String

values := []
for a in range(2, 101)
    for b in range(2, 101)
        n := bigPower(a, b)

        values = n.digits() :: values

values = uniquify(cmpList, values)
print $ show $ values.length()

# Solves Project Euler #29
import BigInt
import List
import String

values := []
for a in range(2, 101)
    for b in range(2, 101)
        n := bigPower(a, b)

        values = values.insert(n.digits())

values = uniquify(values)
println $ showUInt $ values.length()

# Solve Project Euler 5

def gcd a b :: Int =
	if b == 0 then
		return a
	else
		return gcd b (a mod b)

def lcm a b :: Int =
    return a * b / gcd a b

let n :: Int = 1
let i :: Int = 1
while i <= 20 do
	n = lcm n i
	i = i + 1

print n

impl<T> [T]
    def myAt(self: [T], n: Int) -> T
        xs := self
        while n > 0
            xs = tail(xs)
            n -= 1

        return head(xs)

xs := [1, 2, 3]
result := xs.myAt(1)

ys := [True, False, True]
if ys.myAt(2)
    result += 1

print $ show(result)

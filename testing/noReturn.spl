def shouldNotFail(path: Bool) -> Int
    if path
        return 1
    else

        return 2

# Should this succeed?
#def shouldNotFail2() -> Int
#    forever
#        println $ "Hello"

x := shouldNotFail(True)
println $ show(x)

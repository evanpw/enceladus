trait Eq
    def eq(self: Self, other: Self) -> Bool

impl Eq for Int
    def eq(self: Int, other: Int) -> Bool
        return self == other

#def test<T: Eq>(x: T, y: T) -> Bool
#    return x.eq(y)

#if test(1, 2)
#    print "Same"
#else
#    print "Different"
